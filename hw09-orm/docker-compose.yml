version: '3.9'

services:
  postgres:
    image: postgres:15
    container_name: otus-postgres
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: otus
      POSTGRES_USER: otus
      POSTGRES_PASSWORD: otus
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: